o [taka, x, y] = q.top();
			// q.pop();
			// if(- taka < dp[x][y])
			// 	continue;
			// rep(i, 1, x - 1){
			// 	int X = x + (- i), Y = y;
			// 	int tak = 1 + i + dp[x][y];
			// 	if(f(X, Y, tak))
			// 		q.emplace(- tak, x, y),
			// 		dp[x][y] = tak;
			// }